# 版本已锁定，与本机可运行环境一致，保证容器可复现。
# 注意：torch 不在此文件中，由 Dockerfile 用 CPU 专用源单独安装。

# Stage 1: LlamaIndex baseline RAG
llama-index==0.14.21
llama-index-embeddings-huggingface==0.7.0
llama-index-embeddings-openai==0.6.0
llama-index-llms-openai==0.7.7
openai==2.36.0
python-dotenv==1.2.2
pypdf==6.10.2
pandas==3.0.2

# Stage 2: Evaluation with Ragas
ragas==0.4.3
datasets==4.8.5
langchain==1.3.0
langchain-core==1.4.0
langchain-community==0.4.1
langchain-huggingface==1.2.2
langchain-text-splitters==1.1.2

# Stage 3: Agent orchestration with LangGraph
langgraph==1.2.0
langchain-openai==1.2.1

# 模型推理
transformers==5.8.0
sentence-transformers==5.4.1

# 混合检索：BM25 关键词召回
rank-bm25==0.2.2

# Stage 4: Web 服务（FastAPI 后端）
fastapi==0.136.1
pydantic==2.13.4
python-multipart

# Stage 5: PostgreSQL 持久化（psycopg3 驱动 + 连接池）
psycopg[binary]==3.2.10
psycopg-pool==3.2.6
