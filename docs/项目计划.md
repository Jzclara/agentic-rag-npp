# Agentic RAG 项目计划

## 1. 项目实现目的

这个项目的核心目标是从 0 搭建一个可复现、可评估、可展示的 Agentic RAG 系统。它不是只做一个简单的“文档问答 Demo”，而是完整展示一个 RAG 系统从基础检索问答，到 Agent 化控制流，再到量化评估优化的全过程。

项目最终希望解决的问题是：当用户面对一批论文、技术文档或项目资料时，系统可以根据用户问题自动完成检索、判断、重写、重新检索、答案生成和结果评估，最终给出更可靠、更可解释的回答。

这个项目的学习价值主要体现在三个方面：

1. 理解 RAG 的底层流程，包括文档加载、分块、向量化、索引构建、召回和答案生成。
2. 理解 Agentic RAG 的核心思想，即让系统不只是被动检索，而是能够判断检索结果是否足够、是否需要改写问题、是否需要再次检索或走备用路径。
3. 引入评估模块，用指标量化不同方案的效果，而不是只凭主观感觉判断系统是否变好。

## 2. 项目实现方法

整个项目采用“先基线、再 Agent、再优化、最后评估展示”的实现路线。

第一步先实现一个最基础的 RAG 流程。系统读取文档，将文档切分成片段，生成 embedding，建立向量索引。用户提问后，系统根据问题召回相关片段，再把片段和问题一起交给大模型生成答案。这个阶段的重点是跑通最小闭环。

第二步加入评估模块。系统需要记录每次回答时使用的检索上下文、用户问题和最终答案，然后用 Ragas 对回答进行评估。第一版只关注两个指标：faithfulness 和 answer relevancy。faithfulness 用来判断答案是否被检索内容支持，answer relevancy 用来判断答案是否真正回应了用户问题。

第三步使用 LangGraph 将普通 RAG 改造成 Agentic RAG。系统不再是固定的“检索一次然后回答”，而是变成一个可控制的流程图。流程中可以包含问题改写、检索、上下文质量判断、重新检索、答案生成和失败兜底等节点。通过条件边控制不同情况走不同路径。

第四步优化检索层。基础版本跑通后，可以逐步尝试 parent-child chunk、hybrid search、rerank、GraphRAG 等检索增强方法。每一次增强都要重新运行评估，观察指标是否提升。

第五步做一个简单展示入口。可以先用 Streamlit 做一个页面，展示用户问题、最终答案、引用上下文、Agent 执行路径和不同版本的评估结果。这样项目不仅能运行，也方便用于面试、博客或作品集展示。

## 3. 项目实现框架

项目整体可以分为三层：检索层、Agent 编排层和评估层。

### 检索层：LlamaIndex

LlamaIndex 负责文档处理和检索相关工作，包括文档加载、文本分块、embedding、索引构建、向量检索、父子分块检索、混合检索和 rerank 等。它的优势是 RAG 相关能力成熟，适合深入理解检索和索引的细节。

### Agent 编排层：LangGraph

LangGraph 负责 Agent 的控制流设计。它把 RAG 流程拆成多个节点，并通过边和条件边控制系统执行路径。比如：先判断问题是否需要改写，再检索文档，再判断检索内容是否足够，如果不足则重新检索，如果足够则生成答案。LangGraph 的价值在于让 RAG 系统从线性流程变成可观察、可控制、可扩展的图结构。

### 评估层：Ragas

Ragas 负责对系统输出进行量化评估。项目初期只需要使用 faithfulness 和 answer relevancy 两个指标。通过这些指标，可以比较 baseline RAG、Agentic RAG、加入 rerank 后的 RAG、加入 GraphRAG 后的 RAG 等不同版本之间的效果差异。

### 展示层：Streamlit 或简单 Web UI

展示层用于呈现项目结果。初期可以使用 Streamlit，因为它实现快、适合展示实验结果。页面主要展示用户输入、系统答案、引用上下文、Agent 执行路径和评估指标对比。

### 项目目录结构

项目建议采用清晰的分层目录结构。目录不需要一开始就非常复杂，但要能支持后续从 baseline RAG 扩展到 Agentic RAG、评估和展示。

建议项目目录如下：

```text
agentic-rag/
  .venv/
  data/
    raw/
    processed/
  index/
  src/
    config.py
    indexing/
      loaders.py
      chunking.py
      build_index.py
      retriever.py
    rag/
      baseline.py
    graph/
      state.py
      nodes.py
      app.py
    evals/
      run_ragas.py
      testset.jsonl
    ui/
      streamlit_app.py
  results/
    runs/
    evals/
  docs/
    项目计划.md
    技术架构.md
    实验记录.md
  README.md
  requirements.txt
  .env.example
  .gitignore
```

这些目录的作用如下：

1. data/raw 用来存放原始语料，例如 PDF、Markdown、txt 或网页导出的文档。
2. data/processed 用来存放清洗后的中间数据，例如抽取后的文本、分块结果或 metadata。
3. index 用来存放向量索引、文档索引或后续 GraphRAG 相关索引。
4. src/indexing 放文档加载、分块、索引构建和检索逻辑。
5. src/rag 放最基础的 baseline RAG 流程。
6. src/graph 放 LangGraph 相关内容，包括状态定义、节点逻辑和图编排入口。
7. src/evals 放 Ragas 评估脚本和测试集。
8. src/ui 放后续 Streamlit 或 Web UI 展示入口。
9. results 存放每次运行结果、答案记录和评估结果。
10. docs 存放项目计划、技术架构说明和实验记录。

### 项目需要准备的文档

为了让项目更容易复现和展示，建议从一开始就准备几类文档。它们不一定第一天全部写完，但应该随着项目推进逐步补齐。

1. README.md：项目总说明，说明项目目标、技术栈、如何安装、如何运行、当前实现到哪一步。
2. 项目计划.md：记录项目目的、实现路线、阶段安排和当前任务，也就是这份文档。
3. 技术架构.md：记录系统整体架构，包括 LlamaIndex、LangGraph、Ragas 三层分别负责什么，以及数据如何流动。
4. 实验记录.md：记录每次实验做了什么改动、使用了什么语料、评估结果是多少、结论是什么。
5. testset.jsonl：评估问题集，包含用户问题、可选参考答案、问题类型等信息。
6. requirements.txt：记录 Python 依赖，保证别人可以复现环境。
7. .env.example：提供环境变量模板，例如 API key、模型名称、embedding 模型、向量数据库地址等，但不保存真实密钥。
8. .gitignore：排除 .venv、真实 .env、索引缓存、大文件数据和临时结果。

其中最重要的是 README.md、项目计划.md、requirements.txt、.env.example 和 testset.jsonl。它们决定了这个项目是否容易被别人理解和复现。

### 是否需要搭建虚拟环境

需要，建议从第一阶段开始就创建 .venv 虚拟环境。

原因是这个项目会同时使用 LlamaIndex、LangGraph、Ragas、datasets、pandas、Streamlit、向量数据库客户端和大模型 SDK。这些库版本变化比较快，如果不使用独立虚拟环境，很容易和电脑上其他 Python 项目产生依赖冲突。

.venv 的作用是为当前项目提供一个独立的 Python 环境。项目依赖都安装在 .venv 里，不影响系统 Python，也不会影响其他项目。

建议做法是：

1. 在 agentic-rag 项目根目录创建 .venv。
2. 激活 .venv 后再安装项目依赖。
3. 把依赖写入 requirements.txt。
4. 把 .venv 加入 .gitignore，不提交到 Git。
5. 把真实 .env 加入 .gitignore，只提交 .env.example。

第一阶段需要的环境依赖可以先保持最小，只安装 LlamaIndex、大模型 SDK、向量索引相关库和基础数据处理库。等进入第二阶段再加入 Ragas，进入第三阶段再加入 LangGraph，进入第五阶段再加入 Streamlit。这样依赖是随着项目阶段逐步增加的，不会一开始就过重。
## 4. 项目阶段安排

这个项目的阶段安排需要明确体现三层技术分工：LlamaIndex 负责检索层，LangGraph 负责编排层，Ragas 负责评估层。项目不是一开始就把所有框架同时堆上去，而是先把 LlamaIndex 的基础检索闭环跑通，再接入评估，最后用 LangGraph 把流程升级成 Agentic RAG。

可以把整体逻辑理解为：LlamaIndex 是检索发动机，LangGraph 是决策流程，Ragas 是评分系统。

### 第一阶段：用 LlamaIndex 搭建基础 RAG 检索层

目标是完成最小可运行的 baseline RAG。这个阶段主要使用 LlamaIndex，不加入 LangGraph，也不做复杂 Agent 编排。

LlamaIndex 在这一阶段负责：

1. 加载原始文档。
2. 将文档切分成适合检索的文本块。
3. 为文本块生成 embedding。
4. 构建向量索引。
5. 根据用户问题召回 top-k 相关上下文。
6. 将检索上下文交给大模型生成基础回答。

这一阶段的重点是建立一个稳定的检索问答闭环，并保存每次运行的 user_input、retrieved_contexts 和 answer。后续 Ragas 评估和 LangGraph 编排都会基于这些输出继续扩展。

### 第二阶段：为 LlamaIndex Baseline 接入 Ragas 评估

目标是建立项目的评估体系。这个阶段不改变 RAG 流程本身，而是对第一阶段的 LlamaIndex baseline 进行量化评估。

Ragas 在这一阶段负责：

1. 读取测试问题集。
2. 获取 baseline RAG 对每个问题的回答。
3. 使用检索上下文和最终回答计算 faithfulness。
4. 使用用户问题和最终回答计算 answer relevancy。
5. 保存 baseline 的评估结果，作为后续对照组。

这一阶段完成后，项目就不再只是“能回答问题”，而是可以用指标说明回答质量。

### 第三阶段：用 LangGraph 编排 Agentic RAG 流程

目标是把基础 RAG 升级成 Agentic RAG。LangGraph 从这一阶段正式加入，但它不替代 LlamaIndex。LlamaIndex 继续负责检索，LangGraph 负责决定什么时候检索、是否改写问题、是否重新检索、是否生成答案。

LangGraph 在这一阶段负责：

1. 定义 Agent 的状态，例如用户问题、改写后的问题、检索上下文、回答、重试次数等。
2. 将流程拆成多个节点，例如 query rewrite、retrieve、grade context、generate answer、fallback。
3. 使用条件边控制执行路径。
4. 当检索结果不足时，触发问题改写或重新检索。
5. 当检索结果足够时，进入答案生成。

这一阶段的核心价值是让 RAG 从固定线性流程变成可控制、可观察、可扩展的图流程。

### 第四阶段：强化 LlamaIndex 检索层

目标是提升系统的检索质量。这个阶段仍然以 LlamaIndex 为主，LangGraph 的整体流程可以基本保持不变，只替换或增强 retrieve 节点背后的检索能力。

可以逐步尝试的检索增强方法包括：

1. parent-child chunk，用更细的 chunk 做召回，用更完整的父文档片段做回答上下文。
2. hybrid search，结合向量检索和关键词检索。
3. rerank，对初步召回结果重新排序。
4. metadata filtering，根据文档来源、时间、类型等信息过滤结果。
5. GraphRAG 或知识图谱检索，用实体和关系增强复杂问题的召回能力。

每次只建议改一个检索变量，并重新运行 Ragas 评估，观察 faithfulness 和 answer relevancy 是否提升。

### 第五阶段：展示与总结

目标是让项目变成一个可以展示的作品。这个阶段把 LlamaIndex、LangGraph 和 Ragas 的结果整合起来。

展示层可以使用 Streamlit 或简单 Web UI，重点展示：

1. 用户输入的问题。
2. LangGraph 的执行路径，例如是否改写问题、是否重新检索。
3. LlamaIndex 召回的检索上下文。
4. 最终生成的回答。
5. Ragas 对不同版本的评估结果对比。

这一阶段的最终产物不只是一个问答系统，而是一套可以说明“系统如何工作、哪里做了优化、优化是否有效”的完整 Agentic RAG 项目。
## 5. 第一阶段现在要做什么

你现在处于第一阶段，目标是先完成基础 RAG 闭环。这个阶段不要急着加入 LangGraph、GraphRAG 或复杂评估，重点是让系统先稳定跑起来。

第一阶段建议按下面顺序推进：

1. 确定项目语料范围。先选择一个小而稳定的文档集合，例如 5 到 10 篇论文、技术文档或 Markdown 文件。
2. 建立项目目录结构。至少包括 data、src、index、evals、results 这些基础目录。
3. 完成文档加载逻辑。系统需要能够从 data/raw 目录读取文档。
4. 完成文本分块策略。第一版使用普通 chunk 即可，不需要一开始就做 parent-child chunk。
5. 建立向量索引。使用 LlamaIndex 完成 embedding 和索引保存。
6. 实现基础问答流程。用户输入问题后，系统召回 top-k 文本片段，并基于这些片段生成答案。
7. 保存运行结果。每次问答至少记录 user_input、retrieved_contexts、answer，方便第二阶段直接接入 Ragas。

第一阶段的验收标准是：你可以针对文档库提出一个问题，系统能够返回一个基于文档内容的回答，并且可以看到它引用或使用了哪些检索上下文。

如果第一阶段完成得扎实，后面加入 LangGraph 和 Ragas 会非常顺，因为它们本质上都是在这个基础闭环上继续扩展。



