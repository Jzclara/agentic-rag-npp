# Agentic RAG — 核电厂故障诊断智能问答系统

---

## 一、项目介绍与制作动机

### 这是什么项目

这是一个面向**核电厂故障诊断领域**的智能问答系统。用户输入一个技术问题，系统从多篇学术论文中自动检索相关内容，生成带引用标注的专业回答，并支持追溯每条引用到 PDF 原文的具体位置。

### 为什么做这个项目

核电厂故障诊断涉及贝叶斯网络、神经网络、专家系统等多种方法，相关研究分散在大量英文论文中。工程师在实际工作中面临两个痛点：

**痛点一：信息检索效率低。** 故障诊断方法的对比、适用场景、局限性等信息散落在不同论文的不同章节中，人工翻阅耗时且容易遗漏关键信息。

**痛点二：通用 AI 助手不可靠。** 直接用 ChatGPT 等通用模型回答专业问题，容易产生"幻觉"——给出看似合理但实际没有文献支撑的回答，在工程领域这是不可接受的。

### 项目目标

构建一个**可溯源、可验证**的专业问答系统：

- 每句回答都有明确的论文引用来源，点击即可查看原文
- 系统能自主判断检索内容是否充分，不够就重新检索，避免硬编答案
- 通过量化评估指标（而非主观感受）驱动每一步技术改进

---

## 二、具体实现

### 2.1 整体架构

系统采用前后端分离架构，核心是一个具备**自主决策能力**的 RAG（检索增强生成）管线：

```
┌─────────────────────────────────────────────────────────────┐
│  前端 (React + Vite)                                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────────┐  │
│  │ 对话界面  │  │ 推理 Trace│  │ PDF 原文查看 + 文本高亮  │  │
│  └──────────┘  └──────────┘  └──────────────────────────┘  │
│                         │ SSE 流式通信                       │
├─────────────────────────┼───────────────────────────────────┤
│  后端 (FastAPI)         │                                    │
│                         ▼                                    │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  LangGraph 状态图（Agentic RAG 核心）                 │   │
│  │                                                       │   │
│  │  rewrite → retrieve → grade ──→ generate              │   │
│  │              ▲           │                             │   │
│  │              │      (不足) ▼                            │   │
│  │              └──── retry ←┘                            │   │
│  │                     │                                  │   │
│  │                (用完) ▼                                 │   │
│  │                  fallback                              │   │
│  └──────────────────────────────────────────────────────┘   │
│                         │                                    │
│  ┌──────────────────────┼───────────────────────────────┐   │
│  │  检索管线            ▼                                │   │
│  │  向量检索(BGE) + BM25 → RRF 融合 → Cross-encoder 重排│   │
│  └──────────────────────────────────────────────────────┘   │
│                         │                                    │
│  ┌──────────────────────┼───────────────────────────────┐   │
│  │  数据层              ▼                                │   │
│  │  PDF 论文 → 文本提取 → 分块 → BGE Embedding → 向量索引│   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 五阶段迭代过程

项目不是一步到位的，而是经过五个阶段逐步演进。每个阶段解决一个明确的问题。

#### Phase 1：基线 RAG

**做了什么：** 搭建最基本的"检索 + 生成"管线，跑通从论文到问答的完整链路。

##### RAG 搭建流程

一个 RAG 系统的搭建分为两个阶段：**离线索引**和**在线查询**。

**离线索引阶段（只做一次，构建知识库）：**

```
PDF 论文文件
    │
    ▼
① 文档加载：pypdf 逐页提取文本，每页生成一个 Document 对象
   （保留 metadata：文件名、页码，后续引用溯源要用）
    │
    ▼
② 文本分块：SentenceSplitter 把长文本切成 512 token 的小块
   （overlap 50 token，防止关键信息被切断在两个块的边界）
    │
    ▼
③ 向量化：BGE embedding 模型把每个文本块编码为 384 维向量
   （语义相近的文本，向量在空间中距离也近）
    │
    ▼
④ 持久化存储：向量 + 原文 + metadata 写入本地 index/ 目录
   （下次启动直接加载，不用重复做 embedding）
```

**在线查询阶段（每次用户提问时执行）：**

```
用户问题："What fault diagnosis methods are used in NPPs?"
    │
    ▼
① 问题向量化：同一个 BGE 模型把问题编码为 384 维向量
    │
    ▼
② 相似度检索：和索引中所有 chunk 向量做余弦相似度计算，取 top-5
    │
    ▼
③ 上下文拼接：把 5 个最相关的文本块拼接成一段上下文
    │
    ▼
④ LLM 生成：把上下文 + 问题发给 DeepSeek，约束它"只根据上下文回答"
    │
    ▼
返回带引用标注的回答
```

##### 技术选型与备选方案对比

每个环节都有多个可选方案，以下是选择的理由：

###### **① RAG 框架选型：**

**LlamaIndex + LangGraph（而非全部用 LangChain）**

RAG 系统需要解决两个层面的问题：**数据层**（文档怎么加载、切块、建索引、检索）和**控制层**（检索完怎么判断、要不要重试、走哪条路径）。市面上主流框架的定位不同：

| 框架 | 定位 | 数据层能力 | 控制层能力 |
|------|------|-----------|-----------|
| **LlamaIndex** | 专为 RAG 数据管线设计 | 强：内置 SentenceSplitter、VectorStoreIndex、多种向量库适配、持久化，文档到索引一条命令 | 弱：只有简单的 query engine，不支持条件分支和循环 |
| **LangChain** | 通用 AI 应用框架 | 中：有 RecursiveCharacterTextSplitter、VectorStore 封装，但索引管理和持久化需要自己写更多代码 | 中：Chain 支持线性流程，但分支和循环要手写 if-else，代码容易变复杂 |
| **LangGraph** | 有状态的 Agent 编排引擎（LangChain 团队出品） | 无：不处理数据，只编排流程 | 强：StateGraph 天然支持条件边、循环、共享状态，Agent 流程一目了然 |
| 纯手写 | FAISS + 自定义代码 | 灵活但工作量大 | 灵活但工作量大 |

**为什么没有全部用 LangChain？**

LangChain 是"什么都能做"的通用框架，但在这个项目的两个核心需求上，都不是最优选：

- **数据层：** 同样是"加载 PDF → 切块 → 建索引 → 持久化"，LlamaIndex 只需要 `VectorStoreIndex.from_documents(docs)` + `index.storage_context.persist()` 两行代码。LangChain 要分别创建 text_splitter、embedding、vectorstore，再手动管理持久化，代码量多一倍，抽象层级更低。
- **控制层：** 这个项目的 Agentic 流程需要"检索不够 → 改写重试 → 最多 2 次 → 兜底"的循环和分支。LangChain 的 Chain 是线性执行模型（A→B→C），要实现这种循环需要在 Python 层手写 while 循环 + if-else，流程逻辑散落在代码各处。LangGraph 用 `add_conditional_edges` 一行就定义了条件路由，整个流程用图结构表达，可读性和可维护性好得多。

**最终选择：各取所长**

```
LlamaIndex  →  数据层（文档加载、分块、embedding、索引、检索）
LangGraph   →  控制层（rewrite → retrieve → grade → generate 的状态图编排）
```

LangChain 本身没有直接使用，但 LangGraph 是 LangChain 团队的产品，底层部分模块与 LangChain 生态兼容。

###### **② Embedding 模型：**

**BAAI/bge-small-en-v1.5（本地部署）**

Embedding 模型负责把文本编码为向量，是 RAG 检索的基础。常用方案分两大类：

**API 调用型（闭源）：** OpenAI text-embedding-3 系列、Cohere embed-v3、智谱 embedding-3 等。这类模型综合精度高，但有两个问题：一是按 token 计费，频繁调用有成本；二是每个 chunk 的文本都要发送到外部服务器，核电领域论文可能涉及敏感技术信息，存在数据安全风险。因此本项目排除 API 方案，选择本地部署。

**本地部署型（开源）：** 以下是主流选择：

| 模型 | 维度 | 参数量 | 特点 |
|------|------|--------|------|
| **BAAI/bge-small-en-v1.5** ✅ | 384 | 33M | 轻量，CPU 友好，英文检索场景性价比最高 |
| BAAI/bge-base-en-v1.5 | 768 | 109M | 中等体量，精度比 small 高一档，CPU 上仍可运行 |
| BAAI/bge-large-en-v1.5 | 1024 | 335M | BGE 系列最强，但需要 GPU 才能高效运行 |
| intfloat/e5-large-v2（微软） | 1024 | 335M | 和 BGE-large 同级别，MTEB 排名接近 |
| GTE-large（阿里） | 1024 | 335M | MTEB 排名靠前，中英双语能力好 |
| sentence-transformers/all-MiniLM-L6 | 384 | 22M | 最轻量，但精度不如 BGE-small |

**为什么选 BGE-small-en：**

1. **论文是英文的，选英文专用版。** 核电故障诊断论文全是英文，BGE-small-**en** 针对英文优化。如果选中文版或多语言通用模型，在英文学术文本上反而不如英文专用版精确。
2. **同体量下 MTEB 基准表现最好。** MTEB（Massive Text Embedding Benchmark）是 embedding 模型的标准评测。在 384 维 / <50M 参数这个级别，BGE-small-en 在检索任务上的得分高于 all-MiniLM-L6 等同级竞品。
3. **CPU 即可运行，无需 GPU。** 33M 参数，加载不到 1 秒，单个 chunk 编码几毫秒。更大的模型（如 bge-large、e5-large、GTE-large 都是 335M）精度确实更高，但 CPU 上推理慢 5-10 倍，对几十篇论文的原型项目收益不明显。

###### **③ 分块策略：**

**SentenceSplitter（512 token / 50 overlap）**

分块策略直接影响检索质量——切得好，检索到的内容既精准又完整；切得差，要么语义被截断，要么噪声太多。常用的分块方法有以下几种：

| 方法 | 原理 | 语义完整性 | 适合场景 |
|------|------|-----------|---------|
| 固定长度切分 | 按固定 token 数直接切 | 差：会从句子甚至单词中间截断 | 对质量要求不高的快速实验 |
| 递归字符切分 | 按 `\n\n` → `\n` → `.` → 空格依次尝试找分隔符 | 中：尽量在段落或句子边界断开，但不理解句子结构 | 通用场景，LangChain 默认方案 |
| **句子级切分** ✅ | 先拆成完整句子，再组合到接近目标 chunk size | 好：保证不从句子中间截断 | 句子结构完整的文本（学术论文、技术文档） |
| 语义切分 | 用 embedding 计算相邻句子相似度，在"话题转换处"切一刀 | 最好：按语义边界分块 | 话题频繁切换的长文档。但每次分块都要调 embedding，耗时长且块大小不均匀 |
| 按文档结构切分 | 利用 PDF 的标题、章节层级来分块 | 好（格式规范时） | 格式高度统一的文档。学术论文 PDF 格式差异大（分栏、公式），鲁棒性差 |

本项目选择 LlamaIndex 的 SentenceSplitter：学术论文句子结构严谨完整，按句子边界切分最合适，且实现简单可靠。

**为什么 chunk size 选 512 token：**

这个数字是在三个矛盾之间取平衡：

- **太小（128-256 token）：** 检索精度高，但上下文碎片化——LLM 拿到零散片段，缺乏前因后果，回答质量下降。同样的论文切出更多块，检索噪声也增多。
- **太大（1024-2048 token）：** 上下文完整，但一个大块混了多个主题，向量表示被"稀释"，检索精度下降。而且 BGE-small-en 最大输入长度就是 512 token，超过会被截断，embedding 质量劣化。
- **512 token：** 刚好是 BGE-small-en 的最大输入长度，embedding 质量最高；对学术论文来说约覆盖一个完整段落（3-5 句话），语义单元基本完整。50 token 的 overlap 让相邻块有重叠，关键信息在边界处不会丢失。

后续 Phase 4 用 Parent-Child 分块（Child 256 / Parent 1024）进一步解决了这个矛盾——用小块保证检索精度，用大块保证上下文完整，两全其美。

###### **④ 向量存储：**

**LlamaIndex 内置 JSON 存储**

向量存储负责保存 chunk 的 embedding 向量，并在查询时找到最相似的 top-k。常用方案按规模分三档：

| 方案 | 类型 | 检索方式 | 适合规模 | 特点 |
|------|------|---------|---------|------|
| **LlamaIndex 内置** ✅ | JSON 文件 | 暴力搜索（全量余弦相似度） | <10 万条 | 零配置，数据存为 JSON 文件，代码最直观 |
| ChromaDB | 嵌入式数据库 | 近似最近邻（ANN） | 几十万条 | `pip install` 即用，轻量，和内置方案一样不需要额外部署服务 |
| FAISS（Meta） | 本地计算库 | IVF / HNSW 等多种索引 | 百万~千万条 | 性能极强，但只管向量计算，不管 chunk 原文和 metadata 的存储，需要自己写管理代码 |
| Milvus / Qdrant | 分布式数据库 | HNSW / IVF 等 | 千万~亿级 | 生产级，需要 Docker 单独部署数据库服务，支持增删改查、水平扩展 |
| Pinecone | 云托管服务 | 托管 ANN | 任意规模 | 全托管免运维，但数据存在第三方云上 |

**选型理由：当前规模下效果没有差别，选最简单的。**

几十篇论文按 512 token 分块，总共几千个 chunk。几千条 384 维向量做暴力搜索，耗时在毫秒级，和 ChromaDB 的 ANN 近似搜索在速度和检索结果上基本没有区别。暴力搜索反而是精确计算，不存在 ANN 近似搜索可能漏掉候选的问题。

内置方案和 ChromaDB 都能满足需求，最终选内置方案是因为零配置、代码更直观，学习和理解 RAG 流程时不会被数据库的概念分散注意力。

**什么时候需要换：** 如果论文数量增长到上千篇（chunk 达到数十万级），暴力搜索会变慢，那时应迁移到 Milvus 或 Qdrant。LlamaIndex 对这些都有适配器，切换时只需改一行 `StorageContext` 配置，业务代码不用动。

###### **⑤ LLM：**

**DeepSeek-v4-flash**

| 方案 | 特点 | 选择理由 |
|------|------|---------|
| **DeepSeek-v4-flash** ✅ | 性价比高，中英双语能力强，OpenAI 兼容 API | 学术论文是英文，用户可能中文提问，需要双语能力；API 价格低，适合频繁调用 |
| GPT-4 / GPT-4o | 综合能力最强 | 价格高，且对中文学术场景没有决定性优势 |
| GLM-4 (智谱) | 国产，中文能力好 | 早期尝试过，后切换到 DeepSeek 因为推理速度更快 |
| 本地部署 (Qwen/Llama) | 完全离线 | 需要 GPU 资源，且开源模型在复杂推理上和商用 API 仍有差距 |

###### **⑥ Prompt 设计：约束 LLM 的回答行为**

RAG 不只是"把检索到的文本塞给 LLM"——如果不加约束，LLM 可能忽略上下文自由发挥，或者不标注引用来源。

**设计逻辑：** 整个 prompt 由五部分组成——① 设定系统角色（System prompt，约束 LLM 作为 RAG 助手只能基于检索内容回答）→ ② 传入对话历史（让 LLM 理解多轮上下文中的指代关系）→ ③ 传入带编号的检索上下文（`[1]` `[2]` `[3]`，LLM 据此标注引用来源）→ ④ 传入当前问题 → ⑤ 输出要求（使用与用户相同的语言、内联引用标注、证据不足时说不知道）。

具体的约束体现在三个方面：

**约束一：只能基于检索上下文回答，不能编造。**

System prompt 明确要求"Answer based only on the retrieved context"，如果上下文中没有足够证据，要求 LLM 回答"不知道"而不是猜测。这是控制幻觉的第一道防线（第二道是 Phase 3 的 grade 节点）。

**约束二：上下文编号 + 内联引用标注。**

给每段检索到的上下文加编号 `[1]` `[2]` `[3]`，要求 LLM 在回答中用 `[n]` 标记信息来源。例如：

```
检索上下文：
[1] Bayesian networks are widely used for fault diagnosis...
[2] Neural networks can handle nonlinear relationships...

LLM 回答：
贝叶斯网络是核电故障诊断中最常用的方法之一 [1]，而神经网络则擅长处理非线性关系 [2]。
```

这样每句话都可以追溯到具体论文段落，前端再根据 `[n]` 标记渲染成可点击的引用芯片。

**约束三：多轮对话中区分历史和当前上下文。**

Prompt 中把对话历史和当前检索上下文分开传入，明确告诉 LLM："用历史理解用户在问什么，但回答只能基于当前检索到的内容。"防止 LLM 引用之前轮次的过时信息。

**暴露的问题：**

基线 RAG 能跑通，但有两个明显短板：
1. **检索不够准：** 纯向量检索对专业术语（如 "SGTR"、"LOCA" 等故障代码）匹配差，语义相似不等于真正相关
2. **无法自纠：** LLM 不管检索到的内容够不够，都会生成一个"看起来合理"的回答——这在工程领域是危险的

#### Phase 2：Ragas 评估体系

##### 1.为什么先做评估，再做优化？

Phase 1 暴露了"检索不准"和"无法自纠"两个问题，自然的反应是马上动手优化检索。但如果没有一个量化评估基准，优化就变成了凭感觉调参——改完之后"感觉好了一点"，但无法确定是真的好了还是恰好遇到了一个表现好的 case。

所以在动任何优化之前，先建立评估体系：构建固定测试集，跑出 baseline 分数，后续每一步改动都拿同一套测试集重新跑，分数可以直接对比。这样每一步优化都有数据支撑，而不是靠主观判断。

##### 2.RAG 评估框架选型

RAG 系统的评估不能只靠人工看几个 case，需要自动化框架来批量打分。常用的 RAG 评估框架有以下几个：

| 框架 | 定位 | 核心指标 | 特点 |
|------|------|---------|------|
| **Ragas** ✅ | 专注 RAG 评估 | Faithfulness、Answer Relevancy 等 | RAG 评估领域最早最成熟的框架，社区活跃，指标定义针对 RAG 场景设计 |
| DeepEval | 通用 LLM 评估 | 覆盖幻觉、毒性、偏见等 20+ 指标 | 指标覆盖面广，但配置复杂，部分功能需付费账号 |
| TruLens | 可观测性 + 评估 | 和 Ragas 类似的 RAG 三元组指标 | 侧重运行时追踪和监控，评估只是其中一个模块，整体偏重 |
| ARES | 学术研究框架 | 自动化评估 + 统计置信度 | 学术论文导向，自动化程度高，但社区小，文档不完善 |

**为什么选 Ragas：** 本项目的评估需求很明确——衡量"回答是否忠于检索内容"和"回答是否切题"。Ragas 的 Faithfulness 和 Answer Relevancy 两个指标正好对应这两个需求，不需要 DeepEval 那么多的通用指标。同时 Ragas 和项目已有的 LlamaIndex + HuggingFace 生态兼容好，接入成本最低。

##### 3.两个核心指标怎么算的？

Ragas 评估需要三元组输入：**(问题, 回答, 检索上下文)**，然后用 LLM 自动打分。

**Faithfulness（忠实度）—— 衡量幻觉程度：**

```
回答文本
   │
   ▼
① Ragas 用 LLM 把回答拆解成一个个独立论断（claims）
   例如："BN 适用于故障诊断" → claim 1
        "BN 能处理不确定性" → claim 2
   │
   ▼
② 逐条检查每个论断是否能在检索上下文中找到支撑证据
   │
   ▼
③ Faithfulness = 有证据支撑的论断数 / 总论断数
```

得分越高，说明回答越"忠于"检索到的内容，幻觉越少。如果 LLM 编造了上下文里没有的信息，对应的论断就会被判为无支撑，拉低分数。

**Answer Relevancy（回答相关性）—— 衡量是否切题：**

```
回答文本
   │
   ▼
① Ragas 从回答中反向生成多个假设性问题
   例如回答讲了"BN 的优缺点" → 生成 "What are the advantages of BN?"
   │
   ▼
② 计算这些假设问题和原始用户问题的 embedding 相似度
   │
   ▼
③ Answer Relevancy = 假设问题与原始问题的平均相似度
```

如果回答切题，生成的假设问题应该和用户原始问题语义接近，相似度就高。如果回答跑偏或包含大量无关内容，生成的假设问题会偏离原始问题，分数就低。

##### 评估流程

```
固定问题集（testset.jsonl）
   │
   ▼
系统自动运行多轮对话（rewrite → 检索 → 生成）
   │
   ▼
收集每一轮的 (问题, 回答, 检索上下文) 三元组
   │
   ▼
Ragas 用 LLM 对每个三元组自动打分
   │
   ▼
输出 Faithfulness 和 Answer Relevancy 的平均分，保存到 results/evals/
```

同一套测试集分别用 Baseline（线性 RAG）和 Agentic（LangGraph 图）跑一次，两组分数直接对比，就能量化 Agentic 改进带来的效果提升。

##### 为什么只选这两个指标？

Ragas 提供的指标不止这两个，完整的常用指标分为两类：

**回答质量指标：**

| 指标 | 评估什么 | 是否需要标准答案 |
|------|---------|:---:|
| **Faithfulness（忠实度）** ✅ | 回答是否忠于检索内容（幻觉控制） | 否 |
| **Answer Relevancy（回答相关性）** ✅ | 回答是否切题 | 否 |
| Answer Correctness（回答正确性） | 回答是否事实正确 | 是 |
| Answer Semantic Similarity（回答语义相似度） | 回答和标准答案的语义相似度 | 是 |

**检索质量指标：**

| 指标 | 评估什么 | 是否需要标准答案 |
|------|---------|:---:|
| Context Precision（上下文精确度） | 检索到的内容中，相关的排得靠不靠前 | 是 |
| Context Recall（上下文召回率） | 回答所需的信息是否都被检索到了 | 是 |

**只选 Faithfulness 和 Answer Relevancy 的原因：其他指标都需要标准答案（ground truth），这两个不需要。**

Answer Correctness、Context Precision、Context Recall 等指标都要求事先为每个问题准备好"正确答案"。对于核电故障诊断这种专业领域，编写标准答案本身就需要领域专家，工作量大且难以保证质量。

而 Faithfulness 和 Answer Relevancy 只需要 **(问题, 回答, 检索上下文)** 三元组，系统自动产生，不需要人工标注。这两个指标刚好覆盖了 RAG 最关键的两个质量维度：**不编造**（Faithfulness）和**不跑题**（Answer Relevancy），在没有领域专家标注的条件下，是最实用的评估组合。

#### Phase 3：LangGraph Agentic RAG

**做了什么：** 把线性管线改造为带自主决策能力的状态机。

**核心改进：** 增加 grade 节点——让 LLM 判断检索到的内容是否与问题相关。不相关则改写问题重新检索（最多重试 2 次），重试用完走兜底回答，不编造内容。

**LangGraph 状态图定义：**

```
节点：rewrite → retrieve → grade → generate / fallback
路由：grade 之后根据 context_sufficient 字段决定走向
状态：AgentState（TypedDict），包含 question、history、contexts、retry_count 等字段
```

##### ① Agentic RAG 和普通 RAG 的区别

普通 RAG 是线性管线：检索 → 生成，不管检索到的内容质量如何，LLM 都会硬写一个回答。Agentic RAG 的核心区别在于**引入了自我纠正能力**——检索完之后，grade 节点让 LLM 判断"检索到的内容和问题相关吗？"如果不相关，改写问题重新检索；如果重试用完还不行，走 fallback 诚实回答"证据不足"。

本质上是把 RAG 从"单次执行"变成"带条件分支和循环的状态机"——系统能根据中间结果做出不同决策，而不是一条路走到底。

##### ② AgentState：节点间的"共享白板"

LangGraph 的核心通信机制是 **AgentState**（一个 TypedDict）。rewrite、retrieve、grade、generate、fallback 这些节点彼此不直接调用，而是通过一块"共享白板"交流：每个节点进来时从 State 上读取自己需要的信息，完成工作后把结果写回 State，下一个节点再来读。

例如：rewrite 节点从 State 读取 `question` 和 `history`，改写后把 `standalone_query` 写回 State；retrieve 节点从 State 读取 `standalone_query`，检索后把 `contexts` 写回 State；grade 节点读取 `contexts` 做判断，把 `context_sufficient` 写回 State。

**AgentState 在代码中的使用：**
- `state.py`：定义 State 结构（有哪些字段、什么类型）
- `app.py`：构建图，把节点挂到 State 上，定义条件边的路由逻辑
- `server.py`：每次用户请求时创建一个新的 State 实例，填入 question 和 history，交给图执行

##### ③ 预设工作流 vs. ReAct 式 Agent

需要坦诚说明：这个系统的所有分支和循环都是**在构建图时预先定义好的**——rewrite 之后一定是 retrieve，retrieve 之后一定是 grade，grade 之后根据结果走 generate 或 retry。每条可能的路径在代码里都写得清清楚楚。

这和 ReAct 模式的 Agent 不同。ReAct 让 LLM 自己决定"下一步调用什么工具"、"什么时候停止"，路径在运行时动态生成。本系统更准确的定位是**"带条件分支的自动化工作流"**，而不是一个开放式的自主 Agent。

但对 RAG 场景来说，预设工作流反而**更合适**——你不会希望 LLM "创造性地"决定跳过检索直接回答，或者无限循环重试。确定性的流程保证了系统行为可预测、可调试。

##### ④ 为什么最多重试 2 次

重试次数上限设为 2 次，是在三个因素之间取平衡：

- **成本：** 每次重试包含一次 LLM 改写 + 一次全量检索 + 一次 LLM 评判，三次 LLM 调用。重试越多，API 成本越高。
- **延迟：** 用户在等待回答。每次重试增加 2-3 秒延迟，超过两次用户体验明显变差。
- **收益递减：** 如果两次不同角度的改写检索都失败了，说明知识库中很可能没有相关内容，再试也不会有本质改变。此时 fallback 诚实说"证据不足"比继续硬检索更合理。

##### ⑤ 关键设计决策

grade 的判断标准故意放宽——只拦截"完全不相关"，不拦截"部分相关"。因为早期实验中用"sufficient（充分）"标准太严格，频繁误触重试，LLM 反复改写后检索质量反而下降。放宽到"relevance（相关性）"后，重试只在真正检索失败时触发。

##### ⑥ 多轮对话支持

rewrite 节点利用对话历史解析指代词。例如用户先问"What methods are used for fault diagnosis?"，再问"What are their limitations?"，rewrite 会把"their"解析为具体方法名，生成独立的检索查询。

##### ⑦ Fallback 的具体行为

Fallback 不会拿已有的部分检索内容凑一个回答，而是**完全放弃生成**，直接返回一段固定的提示："I'm sorry, I couldn't find enough relevant information in the documents to answer your question." 这是刻意的设计——既然 grade 已经连续判定检索内容不相关，再拿这些内容让 LLM 硬写，只会产生低质量的幻觉回答。在工程领域，"不知道"比"编一个"更负责。

##### ⑧ 为什么 grade 单独做一个节点，而不是让 generate 自己判断

一个自然的想法是：不用单独的 grade 节点，直接在 generate 的 prompt 里加一句"如果上下文不够就说不知道"。但实践中这样做效果差，原因有两个：

- **LLM 倾向于"有话就说"。** 即使 prompt 要求"证据不足时拒绝回答"，LLM 仍然会从零散的上下文中拼凑出一个"看起来合理"的回答。让同一个 LLM 调用同时承担"判断够不够"和"生成回答"两个任务，判断这一步几乎总会被生成欲望压过。
- **判断失败后无法补救。** 如果合在 generate 里，等生成完才发现内容不够，已经来不及重试了。拆成两步后，grade 在 generate 之前拦截，不合格的直接走 retry 改写重新检索，还有机会拿到更好的内容。

本质上是**职责分离**：grade 专注判断，generate 专注生成，各自的 prompt 可以独立优化，比一个 prompt 塞两个任务效果好。

#### Phase 4：检索增强

**做了什么：** 三项检索优化，逐步提升检索精度。

**优化一：Cross-encoder Rerank**

向量余弦相似度是"粗排"——query 和 chunk 分别编码再比较，精度有限。引入 BGE-reranker（Cross-encoder），同时编码 query 和 chunk，精排取 top-5。

```
粗召回 top-10 → Cross-encoder 逐对打分 → 按新分数排序 → 取 top-5
```

**优化二：混合检索（Hybrid Search）**

向量检索擅长语义匹配，但对精确术语（如故障代码、设备型号"SGTR"）可能漏召回。BM25 基于词频统计，擅长关键词精确匹配。两路互补：

```
向量检索 top-10 ─┐
                  ├→ RRF 融合（k=60）→ top-10 候选 → Cross-encoder Rerank → top-5
BM25 检索 top-10 ─┘
```

RRF（Reciprocal Rank Fusion）公式：对每个 chunk 计算 `score = Σ 1/(k + rank)`，在两路中都排名靠前的 chunk 得分最高。

**优化三：Parent-Child 分块**

小 chunk（256 token）检索更精确，但给 LLM 的上下文太碎片化。大 chunk（1024 token）上下文完整，但检索精度下降。

解决方案：用小块检索，返回大块上下文。

```
PDF → Parent 大块 (1024 token) → 每个 Parent 再切 Child 小块 (256 token)
                                          │
                                     建索引在 Child 上
                                          │
                                     检索命中 Child
                                          │
                                     从 metadata 取出 Parent 文本作为 LLM 上下文
```

#### Phase 5：Web UI 与可视化

**做了什么：** 构建完整的前端界面，让系统的决策过程可观察、可验证。

**核心功能：**

- **对话界面：** 支持多轮对话，回答中的引用标记 [1][2] 可点击跳转到来源面板
- **实时推理 Trace：** 右侧面板实时显示 agent 每一步的执行状态——正在改写、正在检索、grade 判定结果、是否触发重试
- **引用溯源：** 点击来源卡片直接打开 PDF 原文，自动定位并高亮匹配文本段落
- **Ragas 评估面板：** 展示评估分数、对比 baseline 和 agentic 的指标差异
- **对话持久化：** 支持多会话管理，刷新不丢失历史记录

**前后端通信：** 使用 SSE（Server-Sent Events）流式传输。后端 LangGraph 每执行完一个节点就推送一条事件，前端实时渲染，不需要等全部完成。

### 2.3 技术栈总览

| 层级 | 技术 |
|------|------|
| LLM | DeepSeek-v4-flash（OpenAI 兼容接口） |
| Embedding | BAAI/bge-small-en-v1.5（本地部署） |
| Reranker | BAAI/bge-reranker-base（Cross-encoder） |
| 索引框架 | LlamaIndex（VectorStoreIndex + SentenceSplitter） |
| Agent 编排 | LangGraph StateGraph（显式状态机） |
| 评估 | Ragas（Faithfulness + Answer Relevancy） |
| 后端 | FastAPI + SSE 流式传输 |
| 前端 | React + Vite + react-pdf |
| PDF 处理 | pypdf（文本提取）+ pdf.js（前端渲染与高亮） |

### 2.4 项目文件结构

```
agentic-rag-npp/
├── src/
│   ├── config.py              # 全局配置（LLM、Embedding、Reranker 参数）
│   ├── llm.py                 # LLM 客户端初始化
│   ├── server.py              # FastAPI 后端（SSE、聊天持久化、PDF 服务）
│   ├── graph/
│   │   ├── state.py           # AgentState 状态定义
│   │   ├── app.py             # LangGraph 图编排（节点连接、条件路由）
│   │   └── nodes.py           # 节点函数（rewrite、retrieve、grade、generate、fallback）
│   ├── indexing/
│   │   ├── loaders.py         # PDF 文档加载与文本清洗
│   │   ├── chunking.py        # 分块策略（标准、Parent-Child）
│   │   ├── build_index.py     # 向量索引构建与持久化
│   │   └── retriever.py       # 检索器（向量、BM25、RRF 融合、Rerank）
│   ├── rag/
│   │   └── generation.py      # Prompt 构建与 LLM 调用（生成回答、改写问题）
│   └── evals/
│       ├── run_ragas.py       # Ragas 评估脚本（三种模式）
│       └── testset.jsonl      # 固定测试集
├── web/
│   └── src/
│       ├── App.jsx            # 主应用（对话管理、SSE 通信、状态持久化）
│       ├── api.js             # 后端 API 调用封装
│       └── components/
│           ├── Inspector.jsx  # 右侧面板（Trace、来源、评估）
│           ├── PdfViewer.jsx  # PDF 查看器（react-pdf + 文本高亮）
│           └── Sidebar.jsx    # 左侧栏（论文列表、会话管理）
├── data/raw/                  # 原始 PDF 论文
├── index/                     # 持久化向量索引
├── start_demo.bat             # 一键启动演示
└── stop_demo.bat              # 一键停止演示
```
